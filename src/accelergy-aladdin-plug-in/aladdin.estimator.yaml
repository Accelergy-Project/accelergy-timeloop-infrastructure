version: 0.1
cacti_plug_in:                                # A (lower case) name for the plugin (not really used elsewhere)
    module: aladdin_table                     # The (lower case) Python module name, which actually is the name of the file
    class: AladdinTable                       # The class name

